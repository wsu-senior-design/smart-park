# Getting Started with React/Django application

In the project directory, you can run the following command to build the images:

### `docker-compose build`

To start the the nginx server with react/django run:

### `docker-compose up`

